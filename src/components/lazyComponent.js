import React from 'react';

const LazyComponent = () => (
    <div>
      <h2>Lazy Component</h2>
      <p>Este es un componente que se carga de forma diferida.</p>
    </div>
  );

export default LazyComponent;
